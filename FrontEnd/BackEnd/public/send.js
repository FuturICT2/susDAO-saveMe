            const options = {method: 'GET', headers: { 'Content-Type':'application/json'}, body: JSON.stringify()}
            
             function open_scanner ()
             {  location.href = "https://webqr.com/index.html"
                 //call the intent to open the QR code scanner
             }     